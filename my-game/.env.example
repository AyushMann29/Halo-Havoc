# Backend WebSocket server URL
# Local development (default):
VITE_SERVER_URL=http://localhost:3000

# Production (Render) — uncomment and set before building:
# VITE_SERVER_URL=wss://your-app-name.onrender.com
